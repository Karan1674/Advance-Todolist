<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/header.ejs')%>
  </head>

  <body class="flex min-h-[100vh] bg-gray-100 justify-center items-center">

    <div class="min-w-lg mx-auto  p-5">
      <form
        action="/forgetPassword"
        method="post"
        class="flex items-start border border-gray-200 justify-center gap-2 shadow-md rounded-md flex-col p-8 pb-18"
      >
        <h2 class="text-center w-full my-5 mb-12 font-semibold text-2xl">
          Advance TodoList
        </h2>

        <div class="flex w-full justify-center gap-2 flex-col">
          <label for="email" class="text-medium font-medium tracking-wide"
            >Enter Your Register Email</label
          >
          <input
            type="text"
            class="w-full p-2 mb-4 border border-gray-300 rounded"
            name="email"
            required
          />
        </div>

        <button
          type="submit"
          class="w-full bg-green-500 p-2 my-2 text-white font-medium"
        >
          Get OTP
        </button>
      </form>
    </div>

    

  </body>
</html>
